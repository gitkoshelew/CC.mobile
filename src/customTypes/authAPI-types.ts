export type LoginType = {
  email: string;
  password: string;
};

export type RegistrationType = {
  name: string;
  email: string;
  password: string;
  nickname: string;
};

export type AuthType = {
  isAuth: boolean;
};
