import {SafeAreaView} from 'react-native-safe-area-context';
import {Text} from 'react-native';

export const SignIn = () => {
  return (
    <SafeAreaView>
      <Text>Sign In</Text>
    </SafeAreaView>
  );
};
